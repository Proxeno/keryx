<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <Description>SRTP/SRTCP protection (AES-CM + HMAC-SHA1, RFC 3711) from the Keryx WebRTC stack.</Description>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="../Keryx.Core/Keryx.Core.csproj" />
  </ItemGroup>
  <ItemGroup>
    <InternalsVisibleTo Include="Keryx.Srtp.Tests" />
    <!-- Benchmark harness reaches the transform layer directly to measure pure AES-GCM / AES-CM+HMAC
         crypto throughput without the encrypt context's replay/rollover bookkeeping. -->
    <InternalsVisibleTo Include="Keryx.Benchmarks" />
  </ItemGroup>
</Project>
